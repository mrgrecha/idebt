import {
  Container, Header, Content, Button, Text,
} from 'native-base';

export default () => (
  <Container>
    <Header />
    <Content>
      <Button primary><Text> My first button </Text></Button>
    </Content>
  </Container>
);
